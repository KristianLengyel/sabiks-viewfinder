# sabiks-viewfinder

Static site and image scraping helper for Sabik's Viewfinder.

## Content editing

Portfolio photos live in `content/photos.json`.

The Decap CMS admin entry point is `admin/index.html`. After deployment, the admin URL will be `/admin/`. GitHub authentication still needs to be configured on the hosting provider before non-technical editing will work.
